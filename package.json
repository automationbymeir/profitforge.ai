{
  "name": "profitforge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "sst dev",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "build": "npm run build --workspaces",
    "test": "npm test --workspaces",
    "shell": "sst shell"
  },
  "devDependencies": {
    "@pulumi/azure-native": "^2.0.0",
    "@types/node": "^20.11.0",
    "sst": "^3.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "mssql": "^12.2.0"
  }
}
